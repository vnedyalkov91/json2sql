export enum SQLOperators {
  OR = 'OR',
  AND = 'AND',
  'OR NOT' = 'OR NOT',
  'AND NOT' = 'AND NOT'
}

export enum SQLComparators {
  EQUAL = '=',
  '=' = '=',
  NOTEQUAL = '!=',
  '!=' = '!=',
  '<>' = '<>',
  GREATERTHAN = '>',
  '>' = '>',
  LESSTHAN = '<',
  '<' = '<',
  GREATERTHANOREQUAL = '>=',
  '>=' = '>=',
  LESSTHANOREQUAL = '<=',
  '<=' = '<=',
  BETWEEN = 'BETWEEN',
  LIKE = 'LIKE',
  IN = 'IN',
  'NOT IN' = 'NOT IN',
  IS = 'IS',
  'IS NOT' = 'IS NOT'
}
